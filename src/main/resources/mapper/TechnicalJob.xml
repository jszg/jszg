<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--namespace 非常重要：必须是 Mapper 类的全路径-->
<mapper namespace="com.xtuer.mapper.TechnicalJobMappler">
  <select id="findRoots" resultType="TechnicalJob">
        select JOB_ID  as id, NAME as name from t_technical_job t where t.parent_id is null order by t.job_code
  </select>

  <select id="findByParent" resultType="TechnicalJob">
        select JOB_ID  as id, NAME as name  from t_technical_job t where t.parent_id = #{parentId} order by t.job_code
  </select>
</mapper>
